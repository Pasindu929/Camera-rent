server.port=5000

# MongoDB Configuration
# For local MongoDB:
# spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb://localhost:27017/camerarent}

# For MongoDB Atlas (replace with your actual connection string):
# spring.data.mongodb.uri=${SPRING_DATA_MONGODB_URI:mongodb+srv://username:password@cluster.mongodb.net/camerarent?retryWrites=true&w=majority}

# Current setting (defaults to local MongoDB):
spring.data.mongodb.uri=mongodb+srv://<pasindu2001929_db_user>:<Pasindu2001>@cluster0.xdghpvp.mongodb.net/<camerarent>?retryWrites=true&w=majority
spring.data.mongodb.database=<camerarent>.user

# Logging
logging.level.org.springframework.web=INFO
logging.level.org.springframework.data.mongodb=INFO


